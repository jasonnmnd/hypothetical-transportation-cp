import React from "react";

//input: a list of buttons - filter by options
//search: takes in 2 inputs? the filter option, and the inputted text
function SearchBar({buttons, serach}){

    return(
        <div></div>
    );
}
export default SearchBar;